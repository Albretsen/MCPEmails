export default {
  title: '邮件签名：Claude 现在能像你本人一样签名',
  description:
    '只需为每个收件箱设置一次签名，Claude 就会自动为它发送的每一封邮件加上签名：新邮件、回复、转发、草稿和定时发送。支持 Gmail、Outlook、Fastmail、iCloud 以及任意 IMAP 账户。',
  coverAlt:
    'Claude 的邮件签名：为每个收件箱设置一次签名，Claude 就会在所有服务商上为它发送的每条消息签名',
  content: `当 Claude 替你发邮件时，它应该看起来就像是你本人发的。这也包括落款：你的姓名、你的头衔、你的公司信息、那个指向你预约页面的链接。在此之前，你要么得在每条指令里手写签名，要么只能接受结尾略显突兀的邮件。今天这一切改变了：**MCP Emails 现在会自动为 Claude 发送的每条消息加上你的签名。**

签名是**按收件箱**设置的。每个已连接的邮箱各自保留自己的签名，于是创始人的头衔出现在你的工作地址上，而一句简单的"— Sam"出现在你的私人地址上。一旦设好，你就再也不用操心它了。

## 哪些会被签名

设置一次，签名就会自动加到 Claude 从该收件箱发送的所有内容上：

- **新邮件** — \`email_compose\`（操作 \`send\`）。
- **回复和转发** — \`email_compose\`（操作 \`reply\` 和 \`forward\`）。
- **草稿** — \`draft\`，所以你审阅时落款已经在它该在的位置。
- **定时发送** — \`schedule\`，所以排队等到周一早上发出的邮件也是带签名的。

你的指令里无需再多说什么。"回复 Dana，告诉她演示文稿在附件里"就会生成一封完整、带签名的邮件。

## 回复和转发会放对位置

签名最棘手的地方在于位置，而这正是大多数工具栽跟头的地方。在回复或转发时，MCP Emails 会把你的签名放在**你的新正文之后、被引用的会话之前**——正是真人会放的位置——而不是把它孤零零地丢在底部、压在几页历史记录之下。

它对来回往复也很聪明。有一个**回复模式**，让签名不会在 Claude 于同一会话中每次作答时都重复出现。第一条回复带签名；这段对话里快速的后续往复不会堆叠出重复的落款。你得到的是干净的会话，而不是一堵重复姓名的墙。

## 在所有服务商上都能用

这并非仅限 Gmail 的小特性。签名适用于**所有**已连接的服务商：Gmail、Outlook、Fastmail、iCloud 以及任意其他 IMAP 账户。由于 MCP Emails 自己组装外发邮件，无论邮箱在哪里，行为都完全一致。连接一个任意类型的新收件箱，签名就会直接生效。

## 两种设置方式

**1. 仪表盘里的签名编辑器。** 在 MCP Emails 仪表盘中打开一个收件箱，就地编辑它的签名。每个收件箱各有其签名，所以你可以为每个地址赋予合适的语气。

**2. 直接问 Claude。** 有一个新的 \`signature\` 工具，所以你可以用自然语言设置或修改签名，无需离开对话：

> "把我工作收件箱的签名设为：Sam Rivera，创始人，Acme — calendly.com/sam"

Claude 会为该收件箱写好签名，从那以后每次发送都带签名。之后让它更新签名，它也照做。

## 当你想要一条不带签名的消息时

有时签名是多余的：一句一个词的"谢谢！"下面不需要三行联系方式。对于这类一次性情况，有 **\`include_signature\` 选项**：让 Claude 在某一条消息中跳过签名，它就会发送精简版。你保存的签名对其余所有内容依然保持不变。

> "回复办公室最后那条消息'我在路上了'——不要签名。"

## 它如何融入你的其余工作流

签名能嵌入你已经在用的工具里。如果 Claude 正在执行你的晨间分拣并连续发出回复，现在每一封都会带上你的落款，而你无需提及。[自动回复模式](/blog/build-email-auto-responder-mcp-agent)和[分拣与摘要指南](/blog/ai-agent-triage-summarize-inbox)同理——任何以发送结尾的操作，现在都以签名结尾。

而且隐私模型不变：你的邮件依然[从不被存储](/blog/why-email-never-stored-matters)。签名只是每个收件箱的一小段配置；你的邮件仍是实时获取，并在 Claude 拿到的那一刻就被丢弃。

## 常见问题

**签名是按收件箱还是按账户？**
按收件箱。每个已连接的邮箱都有自己的签名，所以你可以为工作地址和私人地址分别签名。

**回复和转发会把签名放对位置吗？**
会。它被放在你的新正文之后、被引用的会话之前，回复模式还能防止它在同一会话的来回往复中重复出现。

**哪些服务商支持签名？**
全部：Gmail、Outlook、Fastmail、iCloud 以及任意其他 IMAP 账户。MCP Emails 负责组装外发邮件，所以各处行为一致。

**我能发送不带签名的消息吗？**
可以。对一次性的精简消息使用 \`include_signature\` 选项（只需让 Claude 跳过签名）。你保存的签名对其余所有内容保持不变。

**我该怎么设置签名？**
两种方式：在仪表盘中按收件箱编辑，或者问 Claude——例如通过新的 \`signature\` 工具说"把我的签名设为……"。

## 小结

为每个收件箱设置一次签名，Claude 从该邮箱发送的每一封邮件（新邮件、回复、转发、草稿或定时发送）都会像你亲手写的那样带签名发出。它在所有服务商上都能用，能在会话中放对位置，并在你想要一条不带签名的回复时通过 \`include_signature\` 自动让路。

[打开你的仪表盘](/dashboard)来设置签名，或者直接告诉 Claude 你的签名该是什么。还没连接？[免费开始](/signup)，大约两分钟即可连接一个收件箱。`,
};
