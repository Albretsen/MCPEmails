{
  "name": "mcpemails-openai-review-tools",
  "private": true,
  "type": "module",
  "description": "Reset the OpenAI app-review demo mailbox to a known fixture set.",
  "engines": { "node": ">=20" },
  "scripts": {
    "dry-run": "node seed-demo-mailbox.mjs --dry-run",
    "check": "node seed-demo-mailbox.mjs --check",
    "test": "node --test fixtures.test.mjs mailbox-ops.test.mjs"
  },
  "dependencies": {
    "imapflow": "^2.0.7"
  },
  "devDependencies": {
    "mailparser": "^3.9.0"
  }
}
